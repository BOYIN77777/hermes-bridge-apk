<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hermesandroid.bridge"
    android:versionCode="1"
    android:versionName="0.2.0" >

    <uses-sdk
        android:minSdkVersion="26"
        android:targetSdkVersion="34" />

    <!-- HTTP server -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Wake screen for background actions -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Overlay HUD -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Foreground service to keep bridge alive -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <!-- Screenshots via MediaProjection (Phase 2 enhancement) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Query all installed apps (Android 11+) -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name="com.hermesandroid.bridge.BridgeApplication"
        android:allowBackup="false"
        android:debuggable="true"
        android:extractNativeLibs="false"
        android:icon="@mipmap/ic_launcher"
        android:label="Hermes Bridge"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.HermesBridge"
        android:usesCleartextTraffic="true" >
        <activity
            android:name="com.hermesandroid.bridge.MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Accessibility service declaration -->
        <service
            android:name="com.hermesandroid.bridge.service.BridgeAccessibilityService"
            android:exported="true"
            android:foregroundServiceType="specialUse|mediaProjection"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE" >
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Notification listener service -->
        <service
            android:name="com.hermesandroid.bridge.service.BridgeNotificationListener"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" >
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>

</manifest>